This session is not fully documented, only the notes that I feel need to be mentioned
The other basics can be found anywhere else
## **Most Important Notes:**
### **NOTE 1:**
**VALID**
```c
#include <stdio.h>
int main() {
	int studentID = 15;
	printf("The Student's ID is %d and He is clever", studentID);
	return 0; }
```
In here You will notice that we wrote the format specifier `%d` in the middle of text.

----------
**NOT VALID**
```c
#include <stdio.h>

int main()
{
	int studentID = 15;
	printf("The Student ID is %d", studentID, "and He is clever");
	return 0;
}
```
This is wrong, Once you have closed your "" then you must type in variables only.
### **Note 2:**

| Specifier    | Description                                       |
| ------------ | ------------------------------------------------- |
| `%i` or `%d` | to deal with signed int numbers                   |
| `%u`         | to deal with unsigned int numbers                 |
| `%hd`        | to deal with signed short numbers                 |
| `%hu`        | to deal with unsigned short numbers               |
| `%c`         | to deal with characters                           |
| `%f`         | to deal with float numbers and (double in printf) |
| `%ld`        | to deal with signed long numbers                  |
| `%lu`        | to deal with unsigned long numbers                |
| `%lld`       | to deal with signed long long numbers             |
| `%llu`       | to deal with unsigned long long numbers           |
| `%lf`        | to deal with double in scanf                      |
| `%Lf`        | to deal with long double                          |
### **Note 3:**
Any incrementing or decrementing on a variable and using the same variable in the same statement is an undefined behavior, You have to make these operation 
**Undefined Behavior**
```c
#include <stdio.h>

int main () {
	unsigned char x = 5;
	printf("%i\t%i\t%i\t", ++x, ++x, ++x);
	return 0;
}
```
**Undefined Behavior**
```c
#include <stdio.h>

int main () {
	unsigned char x = 5;
	printf("%i", x++ +x);
	return 0;
}
```
**Valid**
```c
#include <stdio.h>

int main () {
	unsigned char x = 5, y=2;
	printf("%i", x++ +y);
	return 0;
}
```
### **Note 4:**
#### Understanding Lvalue vs Rvalue:
- **Lvalue (Left value)**: An expression that refers to a **memory location**, i.e., it has an **address**. You can assign a value to it.
    - Example: `x = 5;` → `x` is an lvalue.
- **Rvalue (Right value)**: A **value** that does **not have a memory address**, i.e., it's a **temporary result** or constant.
    - Example: `x = y + 2;` → `y + 2` is an rvalue.  

```c
#include <stdio.h>
int main() {
	int x=1;
	++(x++); }
```
#### What happens here:
- `x++` is a **post-increment**, which:
    - Returns the **current value of x (1)** → This is an **rvalue**.    
    - **Increments x** after the value is used → `x` becomes 2.    
- `++(x++)` tries to apply **pre-increment** (`++`) to the **result of `x++`**, which is an rvalue (a temporary value, not a variable with an address).
##### Error:
You **cannot apply `++` to an rvalue**, because `++` needs to **modify the actual memory** (needs an lvalue).
### **Note 5:**
You can format the code using 
- Right Click
	  - Format using Astyle
	    
### **Note 6:**
**Valid**
```c
#include <stdio.h>
int main() {
    int x = 5, y = 5, z = 3, w;
    z = x == y; //Does not need parantheses
    printf("%i\n", z);
    return 0; }
```

### **Note 7:**
**Precedence**

|Category|Operator|Associativity|
|---|---|---|
|Postfix|`() [] -> . ++ --`|Left to right|
|Unary|`+ - ! ~ ++ -- (type)* & sizeof`|Right to left|
|Multiplicative|`* / %`|Left to right|
|Additive|`+ -`|Left to right|
|Shift|`<< >>`|Left to right|
|Relational|`< <= > >=`|Left to right|
|Equality|`== !=`|Left to right|
|Bitwise AND|`&`|Left to right|
|Bitwise XOR|`^`|Left to right|
|Bitwise OR|`|`|
|Logical AND|`&&`|Left to right|
|Logical OR|`||
|Conditional|`?:`|Right to left|
|Assignment|`= += -= *= /= %= >>= <<= &= ^=|=`|
|Comma|`,`|Left to right|
### **Note 8:**
#### **Initializing Chars**
**Valid**
```c
#include <stdio.h>
int main() { 
	char studentID = 97; //a must be between ''
	printf("The Student ID is %c", studentID); 
	return 0; }
		
//-> The Student ID is a
```
**Valid**
```c
#include <stdio.h>
int main() { 
	char studentID = 'a'; //a must be between ''
	printf("The Student ID is %c", studentID); 
	return 0; }
	

//-> The Student ID is a
```
**Not Valid**
```c 
#include <stdio.h>
int main() { 
	char studentID = a; //a must be between ''
	printf("The Student ID is %c", studentID); 
	return 0; }
	

//-> ERROR
```
### Note 8.1: Storing ASCII Values & Displaying Characters

You can store a character literal directly into an integer variable. The compiler will store its corresponding ASCII integer value. Always remember that single quotes `''` are required, otherwise the compiler will treat it as an uninitialized variable name and throw a syntax error.

#### Example 1: Valid ASCII Assignment to Integer
```c
#include <stdio.h>
int main() {
    int x = 'a'; // Assigns the ASCII value of 'a' (97) to integer x
    
    // Printing the ASCII decimal code
    printf("ASCII value: %d\n", x); // Output: 97
    
    // Printing the character representation using format specifier
    printf("Character: %c\n", x);   // Output: a
    
    // Printing using putchar() function
    putchar(x);                     // Output: a
    
    return 0;
}
```
Example 2: Invalid Assignment (Compilation Error)
```C

#include <stdio.h>

int main() {
    int x = a; // ERROR: Compiler treats 'a' as an undeclared variable, not a character
    return 0;
}
```
### **Note 9:**
```c
#include <stdio.h>
int main() {
    int a=1, b=1, c, d;
    c=a++||b++; 
    /*
    a=1 and in the next statement will be 2, It will not check the rest of the
    statement as it has a two operands to the Logical or gate, then it will not
    increment the b
    then after the statement [a=2, b=1, c=1]
    */
    d=--b && a--;
    /*b is decrimented in the same line, 
	then the rest of the statement is ignored as b=0 and due to the 
    logical and gate, then it is not going to decrement a
    then after the statement [a=2, b=0, c=1, d=0]
    */
    printf("a=%i\nb=%i\nc=%i\nd=%i\n",a,b,c,d);
    return 0; }


//-> a=2
//-> b=0
//-> c=1
//-> d=0
```
### **Note 10:**
**Set decimal Precision:**
```c
#include <stdio.h>
int main() {
	float myFloatNum = 3.5;  
	printf("%f\n", myFloatNum);   // Default will show 6 digits after the decimal 
	//point  
	printf("%.1f\n", myFloatNum); // Only show 1 digit  
	printf("%.2f\n", myFloatNum); // Only show 2 digits  
	printf("%.4f", myFloatNum);   // Only show 4 digits
	return 0; }
```
### **Note 11:**
Bitwise Operators: 
Makes the bitwise logic (and, or, not, xor, ..) on each bit, unlike the logical operators that check the whole logic.
```c
#include <stdio.h>
int main() {
	int a = 1, b = 4, bitwise_result, logical_result;
    logical_result = a && b;
    bitwise_result = a & b;
    printf("%i", logical_result); // output is -> 1
    printf("%i", bitwise_result); // output is -> 0
    return 0; }
```

### **Note 12:**
#### **Shift left (<<):**
It inserts a zero in the least significant bit -- right most bit --  then the rest of the bits are shifted left, this means that the number is doubled due to the decimal binary system, so shifting left is like multiplying the number by 2.
![[Pasted image 20250718161244.png]] 
So shifting the number by n, the result is the number * 2^n
```c
#include <stdio.h>
int main() {
    unsigned char x=15, z;
    z=x<<2; //The X here does not change
    printf("%i\n",x);
    printf("%i\n",z);
    return 0; }
    
--------------------------------------------------------
//-> 15
//-> 60
```

```c
#include <stdio.h>
int main() {
    unsigned char x=64;
    unsigned short z=64;
    x<<=2; 
    // two bits has no space to be hold in as char is only 8 bits and there is no space to hold the rest bits
    z<<=2;
    printf("%i\n",x);
    printf("%i\n",z);
    return 0; }
    
--------------------------------------------------------
//-> 0
//-> 256
```

###### **The shift left has no special case.**
#### **Shift Right(>>):**
It inserts a zero in the most significant bit -- left most bit --  then the rest of the bits are shifted right, this means that the number is divided by 2 due to the decimal binary system, so shifting right is like dividing the number by 2.
![[Pasted image 20250718162530.png]]
So shifting the number by n, the result is the number / 2^n

##### **The shift right has a special case with dealing with signed data types.**
If the variable is negative, then the most significant bit is 1, so it inserts ones instead of zeros when dealing with negative signed, so it keeps the two's complement valid.

```c
#include <stdio.h>
int main() {
    signed char x=63;
    x>>=2;
    printf("x=%i\n",x);
    return 0; }
    
-----------------------------------------------------
//-> x=15
```

```c
#include <stdio.h>
int main() {
    signed char x=-6;
    x>>=2;
    printf("x=%i\n",x);
    return 0; }
-----------------------------------------------------
//-> x=-2
```
### **Note 13:**
It is better to name the constant variables all Uppercase and You can not declare it in a statement and assigning a value to it in another statement.
```c
#include <stdio.h>
int main() {
	const int MINUTES_PER_HOUR;  
	MINUTES_PER_HOUR = 60; // error 
	return 0; }
```

### **Note 14:**
That's why, when working with strings, we often use the `fgets()` function to **read a line of text**. Note that you must include the following arguments: the name of the string variable, `sizeof`(_string_name_), and `stdin`:
```c
#include <stdio.h>
int main() {
	char fullName[30];
	printf("Type your full name: \n");  
	fgets(fullName, sizeof(fullName), stdin);  
	printf("Hello %s", fullName);
	return 0; }
```
